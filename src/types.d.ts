export interface CountryFromAPI {
    name: string,
    alpha3Code: string,
    independent: boolean
}

export interface CountryType {
    id: string
    name: string,
    alpha3Code: string,
    clicked: boolean
    independent: boolean
}
